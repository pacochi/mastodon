en:
  activerecord:
    attributes:
      oauth_authentication:
        provider: Provider
        uid: User ID
    errors:
      models:
        account:
          attributes:
            username:
              invalid: only letters, numbers and underscores
        status:
          attributes:
            reblog:
              taken: is already exists
