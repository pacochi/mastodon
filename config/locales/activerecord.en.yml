en:
  activemodel:
    attributes:
      form/account_filter:
        keyword: Keyword
        search_type: Type
      form/account_filter/search_type:
        ip: IP
        email: Email
        name: Name
  activerecord:
    attributes:
      oauth_authentication:
        provider: Provider
        uid: User ID
      media_attachment:
        file: File
        file_content_type: Content-Type
        file_file_size: File size
    errors:
      models:
        account:
          attributes:
            username:
              invalid: only letters, numbers and underscores
        album:
          attributes:
            image:
              in_between: Your cover image must be less than :max
              invalid: The cover image file is invalid
        music_attachment:
          attributes:
            music:
              invalid: The music file is invalid
            image:
              invalid: The cover image file is invalid
          in_between: Your mp3 and cover image must be less than :max for each
        status:
          attributes:
            reblog:
              taken: of status already exists
        pinned_status:
          attributes:
            status:
              no_permission: isn't own toot
              private: is direct toot
              reblog: is boost
