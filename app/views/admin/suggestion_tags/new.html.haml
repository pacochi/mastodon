- content_for :page_title do
  = t('admin.suggestion.title')

= form_for @suggestion_tag, url:admin_suggestion_tags_path  do |f|
  = f.label :order
  = f.text_field :order
  = f.label :tag_name
  = f.text_field :tag_name
  = f.label :description
  = f.text_field :description
  = f.submit '更新する'

%from
  %table.table
    -# %tr
    -#   %td= '優先順位'
    -#   %td= @tag.order
    -# %tr
    -#   %td= 'ハッシュタグ'
    -#   %td= @tag.tag_name
    -# %tr
    -#   %td= '説明文'
    -#   %td= @tag.description
  %button{type: 'button'}= '削除'
  %button{type: 'submit'}= '保存'
