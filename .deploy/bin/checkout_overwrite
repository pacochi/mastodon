#!/bin/bash -eux
git fetch --prune
git checkout --force ${DEPLOY_COMMIT#origin/} # これをしないとgit symbolic-ref --short HEADがブランチ名を返してくれない
git reset --hard $DEPLOY_COMMIT
git clean -fdx -e vendor -e config/deploy
